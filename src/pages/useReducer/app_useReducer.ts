// import { createContext } from 'react'

// //必须导出，以便页面使用，获取状态，类似connect
// export const AppContext = createContext<any>(null)
import { createContext } from 'react';

export const AppContext = createContext<any>(null)